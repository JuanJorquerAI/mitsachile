{
  "name": "mitsa-frontend",
  "type": "module",
  "version": "1.0.0",
  "description": "MITSA Headless Frontend (Astro SSG + SEO)",
  "scripts": {
    "dev": "astro dev --port 8895",
    "start": "astro dev --port 8895",
    "build": "astro build",
    "preview": "astro preview",
    "test": "node --experimental-strip-types --test tests/**/*.test.mjs",
    "typecheck": "astro check"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.2.1",
    "astro": "^5.0.0",
    "marked": "^18.0.11"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "typescript": "^5.7.2"
  }
}
